export const selectSearchTextValue = (state) => state.filter.searchText;

export const selectSortValue = (state) => state.filter.sortBy;

export const selectPriceRangeValue = (state) => state.filter.rangePrice;
